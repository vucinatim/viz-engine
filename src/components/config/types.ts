// Shared types to avoid circular dependencies

// Enum for the different config types
export enum VType {
  Number = 'number',
  Color = 'color',
  Select = 'select',
  Boolean = 'boolean',
  String = 'string',
  Group = 'group',
  File = 'file',
  Vector3 = 'vector3',
}
