# Production foundation dependency handoff

The approved Human Signal treatment assigns two of the seven required graph
consumers to GAP-01 macro-direction and GAP-02 compositor modulation. The prior
queue placed their implementation after the complete skeleton. `ORDER-01`
corrects that dependency without shrinking the skeleton or inventing private
production behavior.

## Preserved history

Predecessor: `goal-five-render-foundation`, definition
`7d14857310526ac660976fc1a6b5064a5de4fac4bdc02c93bc712e7abf03108a`.
Its exact completed terminal is
`ced1617b977d41b94e18da2fe7f974d302b0bafe`.

| Item  | Completed terminal                         |
| ----- | ------------------------------------------ |
| RH-01 | `ffe08d38ee3d92ee75295f01c99f51a17fe6b53c` |
| RH-02 | `477b8262ae314a22a40a3fea8a473a67001678bf` |
| RH-03 | `2b3e70c7c0ba9c37a9a26ccb28f6fca43401de3f` |
| EC-02 | `9d453674861456cfdbc664123f68d719d8183923` |
| EC-03 | `ced1617b977d41b94e18da2fe7f974d302b0bafe` |

The earlier engine-completion definition and EC-01 terminal
`5e74191135098d79eab0bb1ff02b20056cb77add` remain historical authority and
evidence. No completed item is copied into new lifecycle state. Pending items
in the predecessor remain pending historical queue entries; only the tracked
active pointer chooses continuation. Their immutable definitions and evidence
are not deleted, rewritten or reinterpreted.

## Active order and contract

`goal-five-production-foundation` orders:

1. `ORDER-01`: validate and activate this handoff.
2. `EC-07`: implement portable macro-direction through all canonical surfaces.
3. `EC-08`: implement graph-driven compositor values and inspection.
4. `EC-04`: author the complete low-detail seven-graph production.
5. `EC-05`, then `EC-06`: prove authoring and calibrate representative quality.
6. `EC-09` onward: transition integration and the remaining engine horizon.

EC-09 depends on EC-06, so finishing EC-08 cannot bypass production assembly.
All 24 remaining EC items preserve their exact titles, authority, acceptance,
evidence lanes, affected criteria and typed evidence requirements. Only
dependency, priority and gate placement change. No executable graph count is
satisfied by an empty graph, and score/compositor ownership stays in reusable
engine packages. The seven-graph requirement is unchanged.

## Observation and activation

The program definition and tracked active pointer are the canonical owners of
execution order. No automation prompt, production package, sidecar queue or
copied completion state may become a second owner. Exact before/after comparison
checks all preserved acceptance/evidence fields, unique dependency readiness,
predecessor definition bytes and predecessor terminal/evidence identities.
Simulating only `ORDER-01` completion must make exactly EC-07 ready; simulating
EC-07/EC-08 completion must make exactly EC-04 ready, never EC-09.

The successor was prepared outside the checkout, validated and claimed on the
clean predecessor terminal through the canonical `claimWorkItem` API. Under that
lease, its unchanged bytes were copied to the tracked definition and the active
pointer selected it. This is the existing RH-01 handoff mechanism, not a bypass
of writer admission. No direct lifecycle write or human decision fabrication
occurs. Ordinary program validation, checkpoint checks, independent read-only
review, one commit, completion and release govern activation.

Evidence lane: code-architecture, bound to the exact claim, predecessor terminal,
successor definition and frozen diff. The handoff artifact records all immutable
references and negative readiness comparisons. It does not claim product
semantics, creative quality or any completed Goal Five criterion. No irreducible
human choice is added: dependency correction is within the delegated local
architecture authority. Host scheduling remains app-owned; the runbook's current
prompt now directly states the already-authorized successive 01:00–08:00 cadence.
