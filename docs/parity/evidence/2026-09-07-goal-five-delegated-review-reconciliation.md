# EC-01: Delegated review reconciliation

Date: 2026-09-07. Program: `goal-five-engine-completion`. Item: `EC-01`.
Starting commit: `1ee6f1413557f21b82dbcaddcb2f7162f9a4ae46`.

## Outcome and scope

The [delegated review contract](../goal-five-delegated-review.md) reconciles
Goal Five's certification matrix, sensory mappings, active plans, north-star
assumptions and agent role rules with the product owner's 2026-09-06 delegation.
Twelve criteria now require explicit independent review; one retains the exact
Gate 1 human decision. Thirty-two harnesses remain planned and one is ready.
All 46 product criteria remain pending. No Human Signal project, motion, quality
or performance pass is claimed by this checkpoint.

The historical Gate 1 packet, treatment manifest, generated treatment and decision
request retain their approved bytes. The existing program-scoped append-only
human decision confirms approval at `2026-09-06T14:09:05.252Z`; no human-resolution
command was invoked. Stable criterion and lane IDs remain because immutable
execution definitions depend on them.

The active scheduling prose also reflects the explicit nightly authorization:
daily 01:00–08:00 Europe/Ljubljana, 15-minute opportunities through 07:45, one
checkpoint per wake, wind-down at 07:30, exact recoverable stop by 08:00.
Host automation state was not changed.

## Sensory adequacy and ownership

The initial matrix classified 13 reviews as human and admitted only workflows
and paths. That could not reject incomplete or uninspected creative evidence.
The smallest added observation is typed review admission in the existing Goal
Five certification validator. It binds exact evidence bytes, approved treatment,
Gate 1 decision, implementation ancestry, final candidate, review environment,
canonical manifest project identity, reviewers, coverage, findings and defects.
The existing bundle manifest remains the execution owner; no product schema,
runtime, renderer, editor or new generic sensor framework was introduced.

The review-frame union preserves both the treatment's 15 planned stills and the
matrix's additional endpoints, act midpoints and transition midpoints. Gate 2
remains scoped to its representative passage plus complete skeleton evidence;
later gates inspect the complete work. Each independent reviewer must inspect
the prescribed coverage. Architecture review requires its own diff and ownership
evidence rather than unrelated media.

Applicable checkpoint lanes are `creative-human` and `code-architecture`.
Existing semantic, decoded-media, synchronization, performance, lifecycle and
parity rules are unchanged. No actual production media was evaluated here.

## Independent review and corrections

Read-only sensory auditor `sensory_contract` identified the 13-to-12-plus-1
mapping, typed evidence need, historical Gate 1 identity constraint and still
coverage discrepancy before the observation was implemented.

Read-only canonicalizer `canonical_review` and checker `acceptance_review`
independently found an admission defect: pooled coverage permitted a checker
with findings but no artifact inspections. Both reviewers now require complete
independent coverage, protected by a rejection test. The checker also reproduced
string-valued still frames being accepted through substring lookup; frames and
motion/audio ranges now require bounded integer arrays.

The canonicalizer rejected an initial flat review-manifest shape. The final
implementation binds the existing `VizExecutionManifest` kind, schema and nested
project content identity, with commit, environment and revision in review
context. A real-shape fixture passes and a four-field replacement fails. The
checker additionally rejected two reviewers inspecting different renderer
manifest identities for the same project; each gate now requires one manifest
content identity across reviewers, with a substitution rejection test. The
remaining audio scheduling sentence was clarified to distinguish silent analysis
from audible listening. Independent re-review closes those architecture and
acceptance findings; repository checkpoint validation remains a separate gate.

## Validation and closure

- Initial `pnpm check:fast` passed before implementation.
- Initial `pnpm check:focused` passed, retaining its exact historical diff and
  claim-bound record; subsequent reviewer corrections supersede that snapshot.
- The final targeted certification test run passed all 20 tests, covering typed
  evidence requirements, both independent reviewers, missing still/motion/audio/
  project observations, score/source-only evidence, favorable frames, required
  transition midpoints, invalid frame types, stale bytes, wrong candidate/project/
  environment, Gate 1 identity, complete Gates 2–5 history, self-review,
  unresolved defects, manifest substitution and architecture-only evidence.
- Closure requires `pnpm canonicalize` and the complete `pnpm check:checkpoint`
  stage on the final diff. Its machine record is admitted as
  `delegated-review-check` by `program complete`, binding program definition,
  claim, lease, final changed bytes and the one terminal commit. The post-closure
  `repo review create` packet records those immutable evidence identities.

The exact terminal commit, admitted command record, released ownership and review
packet are canonical operational state, not guessed future hashes in this file.
Read them through `pnpm --silent run repo -- program inspect EC-01 --json` and the
post-closure review packet. The next dependency-ready item is `EC-02`; this wake
starts no second checkpoint.

## Limits and remaining work

Typed receipts prove structural coverage and byte identity. They cannot prove
reviewer truthfulness, semantic relevance of a mislabeled artifact, audible
listening or artistic quality. Independent reviewers remain responsible for
actual inspection and judgment. The synthetic admission fixtures are not media
or creative-quality evidence. All production, editor, runtime and final media
proof still belongs to later engine checkpoints. No additional architecture debt
was identified in this bounded review; the engine horizon remains unfinished.
